program MyGl;

uses
  Forms,
  Unit2 in 'Unit2.pas' {MainForm},
  mgl in 'mgl.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TMainForm, MainForm);
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
